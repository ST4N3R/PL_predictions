import streamlit as st
import os

URL_BEGGINING = st.secrets["URL_BEGGINING"]
DATA_PATH = st.secrets["DATA_PATH"]
CURRENT_SEASON = st.secrets["CURRENT_SEASON"]
LEAGUEDB_CONNECTION_STR1 = st.secrets["LEAGUEDB_CONNECTION_STR1"]
LEAGUEDB_CONTAINER_NAME = st.secrets["LEAGUEDB_CONTAINER_NAME"]

LAST_SCRAPPED_MATCH_DATE = st.secrets["LAST_SCRAPPED_MATCH_DATE"]
LAST_SCRAPPED_MATCH_HOUR = st.secrets["LAST_SCRAPPED_MATCH_HOUR"]

AZURE_SQL_CONNECTIONSTRING = st.secrets["AZURE_SQL_CONNECTIONSTRING"]